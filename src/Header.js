import React from 'react';
import {Button, Form, FormLabel, FormText,} from 'react-bootstrap'

function Header() {
    return (
        <div>
            <h1>Voice Verification App</h1>
        </div>
    );
}

export default Header;
